#!/bin/sh
# PFF — Auto-push to origin/main after every commit so Netlify deploys immediately.
# Only pushes when current branch is main.

branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ]; then
  git push origin main
fi
