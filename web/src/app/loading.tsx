import { SovereignSkeletonPage } from '@/components/ui/SovereignSkeleton';

/**
 * Root loading — instant visual feedback during page transitions.
 * Sovereign aesthetic skeleton.
 */
export default function RootLoading() {
  return <SovereignSkeletonPage />;
}
