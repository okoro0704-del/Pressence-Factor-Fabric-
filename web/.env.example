# PFF Web — Environment variables
# Copy to .env.local and fill in values.
# For Netlify: set the same variables in Site settings → Environment variables.

# Format: NEXT_PUBLIC_SUPABASE_URL must be https://*.supabase.co; ANON_KEY must be a valid JWT string.

# Supabase (required for National Pulse realtime and wealth ticker)
# Get from: Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Global SMS Gateway (Section CDLXXII — The Sovereign Bridge)
# Required for OTP on Netlify and local when using Supabase Auth with external SMS.
# Supabase uses these when signInWithOtp(phone) is called; E.164 ensures 080... (Nigeria) and +234 80... resolve to the same Unique Identity Anchor.
SUPABASE_AUTH_EXTERNAL_SMS_PROVIDER=twilio
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token

# Optional: Backend API (for vitalize/register, sync-presence, etc.)
# PFF_BACKEND_URL=http://localhost:4000

# External Sentinel activation URL (Secure Redirect from Main PFF → pffsentinel.com).
# If unset, defaults to https://pffsentinel.com.
# NEXT_PUBLIC_SENTINEL_ACTIVATE_URL=https://pffsentinel.com

# Main PFF App URL (set on the Sentinel site only; used to POST success webhook after payment).
# NEXT_PUBLIC_MAIN_PFF_APP_URL=https://your-main-pff-domain.com

# Optional: shared secret for Sentinel → Main webhook (Authorization: Bearer <secret>).
# SENTINEL_WEBHOOK_SECRET=your-shared-secret

# External Sentinel Vault URL (Redirection Gate: "Go to Sentinel Vault" button).
# If unset, defaults to same-origin /sentinel-vault.
# NEXT_PUBLIC_SENTINEL_VAULT_URL=https://sentinel-vault.example.com

# Government Treasury Dashboard — only users with role GOVERNMENT_ADMIN can access /government/treasury.
# Option A: set localStorage.pff_user_role = 'GOVERNMENT_ADMIN' (e.g. via admin flow).
# Option B: comma-separated pff_ids allowed to access (must also set localStorage.pff_id).
# NEXT_PUBLIC_GOVERNMENT_ADMIN_PFF_IDS=pff_abc123,pff_def456

# Admin Settings (hidden page /admin/settings) — only this identity anchor can access and use "Force Global Schema Refresh".
# Set to your phone (E.164) to allow schema cache refresh via NOTIFY pgrst, 'reload schema'.
# NEXT_PUBLIC_ADMIN_PHONE=+2348012345678

# BETA Liquidity Test: 100% liquidity unlocked (no Sentinel fee), full 1 VIDA spendable, Swap/Send active without Sentinel, direct Sovryn AMM link.
# Set to 1 to enable. In Supabase Edge Function gasless-mint, set BETA_LIQUIDITY_TEST=1 to mint full 5 VIDA to citizen (no 0.1 VIDA to Sentinel).
# NEXT_PUBLIC_BETA_LIQUIDITY_TEST=1

# ============================================================================
# PFF VAULT ADDRESSES (RSK Mainnet)
# ============================================================================
# National Vault: receives 5 VIDA on verification (70% locked, 30% liquid)
# NEXT_PUBLIC_NATIONAL_VAULT_ADDRESS=0x...

# Sentinel Wallet (Hub): receives 0.1 VIDA ($100) from citizen spendable
# NEXT_PUBLIC_SENTINEL_WALLET_ADDRESS=0x...

# Foundation Vault: receives 1 VIDA per vitalization + 2% conversion levy + 5% corporate royalty
# Set this to your deployed Foundation Vault contract address on RSK
# NEXT_PUBLIC_FOUNDATION_VAULT_ADDRESS=0x...

# VIDA Token Contract: mints 5 VIDA to verified user wallet
# NEXT_PUBLIC_VIDA_TOKEN_ADDRESS=0x...

# Architect's Secret Reset: triple-tap on logo/PFF Dashboard triggers nuclear reset (sign out, clear storage, reload).
# Always on in development. In production, set to 1 or true to enable as a hidden field feature.
# NEXT_PUBLIC_ARCHITECT_RESET_ENABLED=1

# ——— Sovereign Shield (production vs sandbox) ———
# Set to your official custom domain hostname (e.g. app.purefreedomfoundation.org) for the LIVE site.
# When hostname matches: Public Mode (no OSINT search), no debug panels, no sensitive logs.
# Netlify .app URL and localhost remain Architect Mode (full search + sandbox for testing).
# NEXT_PUBLIC_PRODUCTION_DOMAIN=app.purefreedomfoundation.org

# Canonical URL for Grand Unveiling (SEO). Set only on custom domain; leave unset on Netlify so testing URL works.
# NEXT_PUBLIC_CANONICAL_ORIGIN=https://app.purefreedomfoundation.org

# ——— SOVRYN Internet Sight (OSINT) ———
# Set one or more keys so the Sovereign Companion can "scan the digital archives" when a user shares their name.
# Paste your key(s) in .env.local (local) or Netlify → Site settings → Environment variables (production).
# See docs/SOVRYN-INTERNET-SIGHT.md for where to obtain keys.
# SERPER_API_KEY=your-serper-api-key
# TAVILY_API_KEY=your-tavily-api-key
# GOOGLE_SEARCH_API_KEY=your-google-custom-search-api-key
