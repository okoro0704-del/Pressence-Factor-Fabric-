# PFF Web — Environment variables
# Copy to .env.local and fill in values.
# For Netlify: set the same variables in Site settings → Environment variables.

# Format: NEXT_PUBLIC_SUPABASE_URL must be https://*.supabase.co; ANON_KEY must be a valid JWT string.

# Supabase (required for National Pulse realtime and wealth ticker)
# Get from: Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Global SMS Gateway (Section CDLXXII — The Sovereign Bridge)
# Required for OTP on Netlify and local when using Supabase Auth with external SMS.
# Supabase uses these when signInWithOtp(phone) is called; E.164 ensures 080... (Nigeria) and +234 80... resolve to the same Unique Identity Anchor.
SUPABASE_AUTH_EXTERNAL_SMS_PROVIDER=twilio
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token

# Optional: Backend API (for vitalize/register, sync-presence, etc.)
# PFF_BACKEND_URL=http://localhost:4000

# External Sentinel activation URL (Secure Redirect from Main PFF → pffsentinel.com).
# If unset, defaults to https://pffsentinel.com.
# NEXT_PUBLIC_SENTINEL_ACTIVATE_URL=https://pffsentinel.com

# Main PFF App URL (set on the Sentinel site only; used to POST success webhook after payment).
# NEXT_PUBLIC_MAIN_PFF_APP_URL=https://your-main-pff-domain.com

# Optional: shared secret for Sentinel → Main webhook (Authorization: Bearer <secret>).
# SENTINEL_WEBHOOK_SECRET=your-shared-secret

# External Sentinel Vault URL (Redirection Gate: "Go to Sentinel Vault" button).
# If unset, defaults to same-origin /sentinel-vault.
# NEXT_PUBLIC_SENTINEL_VAULT_URL=https://sentinel-vault.example.com

# Government Treasury Dashboard — only users with role GOVERNMENT_ADMIN can access /government/treasury.
# Option A: set localStorage.pff_user_role = 'GOVERNMENT_ADMIN' (e.g. via admin flow).
# Option B: comma-separated pff_ids allowed to access (must also set localStorage.pff_id).
# NEXT_PUBLIC_GOVERNMENT_ADMIN_PFF_IDS=pff_abc123,pff_def456
