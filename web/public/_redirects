# API routes → Netlify Functions (must come before catch-all so /api/* is not sent to SPA)
/api/sovereign-recognition    /.netlify/functions/sovereign-recognition    200
/api/sovereign-recognition/   /.netlify/functions/sovereign-recognition    200
/api/identity-reset           /.netlify/functions/identity-reset          200
/api/identity-reset/          /.netlify/functions/identity-reset           200

# No redirect: Treasury and Vitalization are directly accessible. Only sign-in and device-approval require vitalization; no 9-day ritual or countdown gate.
# SPA fallback: Netlify serves out/<path>/index.html for /<path>/ and /<path> automatically
